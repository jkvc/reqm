# Scripts — uniform call sites that demonstrate the QuantManager workflow.
#
# Each script uses the same pattern: build an Estimator from a config name,
# then do something with it. The config name is the only thing that changes.
#
# Run any script with:
#     uv run python -m examples.estimators.scripts.<script_name> [args]
